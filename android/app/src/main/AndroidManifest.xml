<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Reminders (U1/U2/U12): POST_NOTIFICATIONS is merged in by
         flutter_local_notifications itself; RECEIVE_BOOT_COMPLETED lets the
         plugin's boot receiver re-schedule pending reminders after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Drive backup sync (U24): the app's only network use. Debug/profile
         manifests carry INTERNET for hot reload; release needs it declared
         explicitly or every Drive call fails with SocketException. Not a
         runtime (user-facing) permission. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Hanna checker direct BLE (U33): scan for and talk to the HI97115
         photometer. neverForLocation because the app derives no location from
         scan results; the legacy trio covers API < 31 where BLE scanning
         rides the location permission. flutter_blue_plus requests the runtime
         grants itself when a scan/connect starts. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30"/>
    <!-- Bluetooth is OPTIONAL: without these, Play infers required hardware
         from the permissions above (the Bluetooth ones imply
         android.hardware.bluetooth; ACCESS_FINE_LOCATION implies location AND
         location.gps) and silently filters the app off devices lacking it.
         Declaring the features non-required restores availability there; the
         Hanna checker entry points hide at runtime when BLE is absent
         (hannaBleSupportedProvider). -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false"/>
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false"/>
    <uses-feature android:name="android.hardware.location" android:required="false"/>
    <uses-feature android:name="android.hardware.location.gps" android:required="false"/>
    <!-- Checker camera scan (U34): the camera plugin merges in the CAMERA
         permission (Play then infers camera + autofocus hardware) AND its own
         `<uses-feature android.hardware.camera.any/>` with no `required`
         attribute — which DEFAULTS TO REQUIRED and dropped 133 camera-less
         devices from the supported list on the 0.37.0 upload. The camera is
         OPTIONAL — all three features are declared non-required (the app
         node wins the merge) and the scan screen shows a "no camera"
         message at runtime instead. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <!-- tools:replace is load-bearing: `required` OR-merges (true wins), so
         without it the plugin's required="true" survives the merge. -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false"
        tools:replace="android:required"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <!-- The camera plugin also merges in RECORD_AUDIO for video capture,
         which the app never uses (enableAudio: false) — and which makes
         Play infer a required microphone. Strip it. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove"/>
    <application
        android:label="ReefTracker"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:allowBackup="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications: delivers scheduled reminders (alarm
             broadcast) and re-schedules them after a reboot. The plugin does
             not declare these itself — removing them silently breaks
             scheduled notifications. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
